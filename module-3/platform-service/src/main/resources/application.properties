# Server Configuration
server.port=8080

# Actuator Endpoints
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Database Configuration
# Note: These values are overridden by environment variables in Docker
spring.datasource.url=jdbc:postgresql://localhost:5432/platform_requests
spring.datasource.username=platform
spring.datasource.password=platform123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false